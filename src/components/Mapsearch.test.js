import React from 'react';
import ReactDOM from 'react-dom';
import Layout from './Layout';

const mapsearch = require('./Mapsearch');
const map = new Map();

test('map loads on startup', () => {
  expect(Map);
});
